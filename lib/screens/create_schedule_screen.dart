import "package:flutter/material.dart";

class CreateScheduleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text(
        "This is create schedule screen",
      )),
    );
  }
}
